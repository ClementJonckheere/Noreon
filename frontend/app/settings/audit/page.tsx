import DomainLanding from "@/components/DomainLanding";
export default function AuditPage() {
  return (
    <DomainLanding
      title="Journal"
      lead="Qui a fait quoi, tracé et signé. Noreon y figure comme un acteur à part entière ; chaque événement automatique est relié à son instantané."
      emptyTitle="Journal vide"
      emptyBody="Les lectures, recalculs, validations et autorisations s'inscrivent ici au fil de l'usage."
    />
  );
}
