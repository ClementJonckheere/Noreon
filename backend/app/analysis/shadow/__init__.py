"""Phase 2 — C5 : Shadow Planner.

Le planner LLM est OBSERVÉ, jamais décisionnaire : le fallback reste le seul
chemin qui produit la réponse rendue à l'utilisateur. Ce package ne contient que
l'observation (projection, comparaison, télémétrie, exécuteur non bloquant).
"""
